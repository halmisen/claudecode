# Core backtest
backtrader==1.9.78.123
pandas==2.3.1
numpy==1.26.4
matplotlib==3.10.5
python-dateutil==2.9.0.post0

# Indicators（默认用 pandas-ta；需要严格对齐再启用 TA-Lib 于本地清单）
pandas-ta>=0.3.14b0
# TA-Lib 请放在 requirements-local.txt（见下）

# Utilities
loguru>=0.6.0
tqdm>=4.64.0

# Interactive plotting（启用交互图；为兼容 backtrader_plotting==2.x 固定 bokeh<3）
plotly>=5.0.0,<7.0.0
bokeh>=2.3,<3.0
backtrader_plotting==2.0.0
# 对超大时序想要更顺滑再考虑：plotly-resampler>=0.10.0（暂不安装）

# Optional（按需手动取消注释再安装）
# yfinance>=0.2.0
# ccxt>=2.0.0
# aiohttp>=3.8.0
# asyncio-mqtt>=0.10.0